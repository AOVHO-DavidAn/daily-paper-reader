---
title: "J-CoT: Chain-of-Thought in J-Space"
title_zh: J-CoT：J空间中的思维链
authors: "Junde Wu, Jiayuan Zhu, Fengling Liu, Minhao Hu, Jiazhen Pan"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.21981v1"
tags: ["query:lr"]
score: 9.0
evidence: 基于J-空间（词汇索引连续空间）的隐空间推理框架
tldr: 现有思维链方法依赖自然语言作为中间接口，限制了推理效率；而潜在推理方法传递完整隐藏状态，缺乏选择性。本文提出J-CoT，在J-space（基于词汇索引的隐空间）中循环传递中间系数，无需语言解码或完整状态传递。实验表明，J-CoT-Zero匹配或超越最强潜在推理基线，J-CoT-Train在数学、科学、编码等任务上取得最高分。该方法高效结合了语言化接口与隐式推理的优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21981v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1430, \"height\": 381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21981v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 573, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21981v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 596, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21981v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1452, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21981v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 718, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21981v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 721, \"height\": 227, \"label\": \"Table\"}]"
motivation: 思维链依赖自然语言接口过于严格，而潜在推理传递完整隐藏状态缺乏选择性。
method: J-CoT在J-space中循环传递词汇索引系数（J-thought），作为语言化且可选择的中间接口。
result: J-CoT-Zero匹配或超越最强潜在推理基线，J-CoT-Train在所有任务上取得最高分。
conclusion: J-CoT提供了高效的语言化隐式推理接口，无需解码或完整状态循环。
---

## 摘要
思维链提示通过跨连续计算步骤传递中间状态来改善语言模型的推理能力。然而，仅依赖自然语言作为唯一的循环接口过于严格，因为许多瞬时计算无需完全言语化。现有的潜在推理方法通过循环传播连续隐藏状态来消除这一限制。然而，这些方法将密集的隐藏向量整体传递，没有显式机制来选择和组织下一步推理所需的信息。这激发了一种中间接口，它在保持语言基础的同时无需解码出完整句子。我们提出了\textbf{J-CoT}，这是一个基于\emph{J空间}的循环推理框架，J空间是模型隐藏表示中的一个词汇索引坐标系。在每个循环内，模型在其完整的隐藏空间中进行计算。在循环边界处，J-CoT将中间状态表示为词汇索引系数，将这些系数作为\emph{J-思维}向前传递，并将其映射回模型的隐藏表示中以供下一循环使用。因此，J-CoT既不需要流畅的中间推理理由，也不需要完整隐藏状态的循环。在匹配的骨干网络和推理设置下，J-CoT-Zero在每个基准测试中达到或超过评估最强的潜在推理基线，而J-CoT-Train在评估的数学、科学、编程和结构化路径推理任务中均获得最高分数。

## Abstract
Chain-of-thought prompting improves language-model reasoning by carrying intermediate states across successive computation steps. However, relying on natural language as the only recurrent interface is overly restrictive, since many transient computations do not need to be fully verbalized. Existing latent-reasoning methods remove this constraint by recurrently propagating continuous hidden states. However, these methods pass a dense hidden vector as a whole, without an explicit mechanism for selecting and organizing the information needed by the next reasoning step. This motivates an intermediate interface that remains linguistically grounded without requiring a decoded sentence. We introduce \textbf{J-CoT}, a recurrent reasoning framework built on \emph{J-space}, a vocabulary-indexed coordinate system within the model's hidden representations. Within each cycle, the model computes in its full hidden space. At the cycle boundary, J-CoT expresses the intermediate state as vocabulary-indexed coefficients, carries these coefficients forward as a \emph{J-thought}, and maps them back into the model's hidden representation for the next cycle. J-CoT therefore requires neither a fluent intermediate rationale nor recurrence over the complete hidden state. Under matched backbone and inference settings, J-CoT-Zero matches or exceeds the strongest evaluated latent-reasoning baseline on every benchmark, while J-CoT-Train obtains the highest score across the evaluated mathematical, scientific, coding, and structured path-reasoning tasks.

---

## 论文详细总结（自动生成）

# J-CoT: Chain-of-Thought in J-Space 论文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：现有的思维链（CoT）方法强制中间状态必须解码为自然语言序列，这限制了模型的内部计算灵活性；而潜在空间推理方法（如Coconut）则传递完整的密集隐藏状态，缺乏选择和显式组织信息的机制。
- **动机**：人类推理中常用非全句式的、语言上可索引的局部假设引导下一步思考。因此需要一种介于完全言语化和完全密集隐状态之间的中间接口，既能保持语言基础性，又不要求完整句子解码。
- **整体含义**：J-CoT通过词汇索引坐标系（J-space）实现这种中间接口，使模型在循环边界传递稀疏的词汇系数状态（J-thought），既保留语言索引的明确性，又允许在循环内部进行自由的密集计算。

## 2. 方法论：核心思想、关键技术细节与流程

- **核心思想**：利用J-space（词汇索引坐标系）作为循环边界上的表达形式。该坐标系通过每层的词典将隐藏状态投影到词汇空间，不同层使用同一套词汇索引，使系数可在层间传输。
- **关键组件**：
  - **J-lens字典**：为每层计算，由Jacobian Jℓ和词嵌入矩阵WU构成，列向量dℓ,v对应词汇v在层ℓ的残差方向。
  - **提取与重建**：通过非负弹性网分解（Eq. 6）从激活中提取系数Φℓ(h)，再通过Ψℓ(a)=Dℓa重建残差。
  - **载波位置（Carrier）**：在输入末尾添加M个非语言位置，负责读写J-thought。
  - **循环机制**（Eq. 7）：每个周期t，先读取前一个J-thoughtAt-1，通过读层字典重建，经门控与基线相加；然后经过读层到写层之间的Transformer块；最后在写层提取新系数形成At。
- **两种配置**：
  - **J-CoT-Zero**：固定载波嵌入和单位读强度，无需针对接口训练。
  - **J-CoT-Train**：训练载波嵌入和两层读门（瓶颈宽度256），冻结Transformer和J-lens字典。
- **自适应终止**：监控J-thought变化的Frobenius范数，连续两次<0.02则终止，最大循环数8（可扩展）。
- **理论保证**：Proposition 1给出多跳稳定性界，条件为提取误差和重建-再提取误差足够小。

## 3. 实验设计：数据集、基准与对比方法

- **数据集**：覆盖数学（GSM8K、MATH-500、AIME 2024）、科学（GPQA-Diamond）、编程（HumanEval+、MBPP+、LiveCodeBench、CRUXEval）共8个基准。
- **基础模型**：主实验用Qwen3-8B-Base，缩放实验扩展到Qwen2.5-7B/14B/32B、Llama-3.1-70B/405B。
- **对比方法**：
  - 语言接口：CoT、PS+（Plan-and-Solve）。
  - 潜在接口：Coconut、CODI、SIM-Coconut。
- **评估指标**：数学/科学用准确率；编程用pass@1（CRUXEval用宏平均）。贪婪解码，单次生成，无自洽性/外部工具。
- **额外实验**：接口谱系实验（在ProsQA上插值密集、J-thought、语言三种接口）；消融实验。

## 4. 资源与算力

- 文中未明确说明具体GPU型号、数量、训练总时长。仅提到J-CoT-Train使用AdamW优化器，学习率2e-4，权重衰减0.01，全局批量128，训练10,000步，最多16个循环展开。
- 所有基线共享同一适应性微调后的Qwen3-8B-Base骨干，J-lens字典在1,000条长度为128的校准序列上估计一次，无需额外大规模预训练。
- 需要指出：论文缺乏具体的计算资源报告（如GPU卡时数），不利于复现和评估可扩展性。

## 5. 实验数量与充分性

- **实验组数**：主实验8个基准×7种方法；缩放实验5个模型×3种深度配置（共15点）；接口谱系实验在ProsQA上9个λ值；消融实验包含两个子表（接口消融4种，结构消融4种）。
- **充分性**：
  - 基准覆盖广泛（数学、科学、编程、路径推理），消除单一领域偏差。
  - 对比方法包括当前最强潜在推理和语言推理基线，且所有方法共享相同的适应性微调骨干，训练数据/分裂一致，控制变量严格。
  - 三个随机种子平均±标准差，报告置信区间，统计严谨。
  - 消融实验直接验证关键设计（读接口、J-thought结构、载波数量等）。
- **公平性**：所有基准使用相同提示模板、解码设置和FLOPs匹配；Coconut/CODI/SIM-Coconut按原始论文协议训练，J-CoT使用相同训练示例/分裂；J-CoT-Zero无需额外训练则更显公平。
- 总体评价：实验设计全面、控制良好，结论可信度高。

## 6. 主要结论与发现

- **性能超越**：J-CoT-Train在所有8个基准上取得最高平均分（50.2%），超过最强潜在基线SIM-Coconut（47.5%）2.7个百分点；J-CoT-Zero（47.9%）也匹配或超过SIM-Coconut。
- **领域一致性**：在数学、科学、编程各子领域均有一致提升（2.1–3.6点）。
- **缩放行为**：随模型容量和循环深度增加，性能持续提升，且更大模型从更深度推理中获益更多（H/16 vs H/4在MATH-500上+18.4点，LiveCodeBench上+13.6点）。
- **接口谱系最优**：在ProsQA上，J-thought接口（λ=0.5）准确率88.8%，明显高于密集端点（84.0%）和语言端点（79.0%），且恢复率高、无效路径持久性低。
- **消融证实设计**：去除J-thought读取、使用学习状态或学习适配器均显著下降；多载波、载波间注意力和自适应系数大小均有贡献。

## 7. 优点

1. **新颖性**：首次提出将词汇索引系数作为循环推理接口，填补了语言化与密集隐状态之间的空白。
2. **理论支撑**：Proposition 1提供多跳稳定性的数学保证，指导实际构造。
3. **无需训练即可工作**：J-CoT-Zero仅使用固定J-lens和简单读取机制，即达到或超越最强潜在基线，说明预训练模型内部已有可用的J-space坐标系统。
4. **轻量训练**：J-CoT-Train只训练载波嵌入和读门（极少量参数），保持骨干冻结，效率高。
5. **兼容性强**：可叠加在多种基础LLM上，缩放实验显示与模型容量正交互。
6. **实验全面**：覆盖多领域、多基线、多模型规模、多深度设置，并有接口谱系和消融验证。

## 8. 不足与局限

1. **算力细节缺失**：未报告具体GPU型号、数量、训练耗时，不利于可重复性和成本估算。
2. **依赖预训练J-lens**：J-lens字典需在额外校准数据上估计，校准数据选择和Jacobian近似误差可能影响结果。
3. **弹性网分解开销**：每循环需解非负弹性网优化，虽然可并行但计算复杂度高于简单全连接，可能增加推理延迟（但FLOPs已在报告中包含）。
4. **训练变体局限**：仅优化载波嵌入和读门，未探索更多参数（如J-lens字典或读层/写层选择），限制了可能的上限。
5. **应用限制**：仅验证于基于Transformer的LLM，未涉及其他架构（如Mamba）；且任务类型限于单步答案推理，未测试多轮对话或长文本生成。
6. **解释性不足**：虽然J-thought系数可对应词汇，但文中未展示具体哪些概念被激活，缺乏定性分析（如案例研究）。
7. **消融覆盖有限**：未消融门控设计、层选择、弹性网正则化强度等超参数敏感性。

（完）
