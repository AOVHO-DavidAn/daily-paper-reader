---
title: Toward Efficient Weakly Supervised Semantic Segmentation Using Only Low-Magnification Histopathological Images
title_zh: 面向仅使用低倍镜组织病理学图像的高效弱监督语义分割
authors: "Dung Minh Do, Nhat-Thanh Huynh, Duc Minh Huynh, Doanh C. Bui, Khang Nguyen"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10783v1"
tags: ["query:seg-llm"]
score: 9.0
evidence: 研究语义分割任务
tldr: 全切片图像高倍率存储和像素级标注成本高。本文模拟低倍率输入并重建至原始尺寸，使用弱监督分割进行系统基准研究。实验发现存在关键退化点，小尺度结构定位显著下降，且重建质量指标不足以预测分割性能。为设计高效数字病理存储系统提供了实用指导，平衡存储效率与自动分析可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10783v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1866, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10783v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1863, \"height\": 2171, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10783v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1871, \"height\": 519, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10783v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1884, \"height\": 555, \"label\": \"Table\"}]"
motivation: 降低高倍率病理图像存储和像素级标注成本，探索低倍率图像和图像级标签在弱监督分割中的可行性。
method: 从高分辨率图像块模拟低倍率输入，使用插值和深度重建方法恢复原始尺寸，再应用弱监督分割管道进行定量评估。
result: 重建质量指标不能预测下游分割性能；存在关键退化点，小尺度结构定位显著下降。
conclusion: 为设计高效数字病理存储系统提供指导，在降低存储需求的同时维持可靠自动分析性能。
---

## 摘要
全切片图像（WSI）提供了丰富的组织级和细胞级信息，但存储和传输高倍镜病理数据资源消耗大。此外，在像素级标注WSI是劳动密集且耗时的。因此，研究具有有限标注（即图像级而非像素级标签）的低倍镜病理图像是否能达到与高倍镜图像相当的性能非常重要。本文针对不同低分辨率存储设置下的弱监督组织病理学图像分割进行了系统性基准研究。从高分辨率图像块开始，我们模拟低倍镜输入，并使用插值和基于深度学习的重建方法将其重建为原始尺寸，然后应用弱监督分割流程。该框架能够定量评估弱监督方法对不同分辨率退化水平的响应。实验结果表明，仅凭重建质量指标不足以预测下游分割性能。特别地，研究识别出一个关键退化点，在此处小尺度结构的定位显著下降。这些发现为设计高效的数字病理存储系统同时保持可靠的自动化分析提供了实用指导。代码可在 https://github.com/Dung-Dx/LowMagWSS 获取。

## Abstract
Whole-slide images (WSIs) provide rich tissue-level and cellular-level information, but storing and transmitting high-magnification pathology data is resource-intensive. Moreover, annotating WSIs at the pixel level is labor-intensive and time-consuming. Therefore, it is important to investigate whether low-magnification pathology images with limited annotations (i.e., image-level instead of pixel-level labels) can achieve performance comparable to high-magnification images. This paper presents a systematic benchmark study on weakly supervised histopathological image segmentation under different low-resolution storage settings. Starting from high-resolution image patches, we simulate lower-magnification inputs and reconstruct them to the original size using interpolation and deep learning-based reconstruction methods before applying the weakly-supervised segmentation pipeline. This framework enables a quantitative evaluation of how weakly supervised methods respond to different levels of resolution degradation. Experimental results show that reconstruction quality metrics alone are insufficient to predict downstream segmentation performance. In particular, the study identifies a critical degradation point where the localization of small-scale structures declines significantly. These findings provide practical guidance for designing efficient digital pathology storage systems while maintaining reliable automated analysis. Code is available at https://github.com/Dung-Dx/LowMagWSS

---

## 论文详细总结（自动生成）

# 面向仅使用低倍镜组织病理学图像的高效弱监督语义分割——论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：全切片图像（WSI）在数字化病理中广泛应用，高倍镜图像（如40×）存储和传输成本高昂，且像素级标注需要大量人工。弱监督语义分割（WSS）可利用图像级标签降低标注负担，但低倍镜图像可能丢失精细组织信息，影响分割性能。因此，需要系统研究低倍镜存储后重建是否能维持下游WSS性能，从而在存储效率和自动化分析可靠性之间取得平衡。
- **核心问题**：低倍镜存储（20×、10×）配合插值或超分辨率（SR）重建后，WSS能否达到与原始40×图像相当的性能？重建质量指标（PSNR、SSIM等）能否预测分割性能？是否存在关键的退化阈值，导致小尺度结构定位完全失效？
- **整体含义**：该研究首次将低倍镜病理图像存储与弱监督分割结合，提出任务驱动评估框架，为数字病理系统的存储策略设计提供实用指导。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：模拟低倍镜存储场景，将高倍镜（40×）病理图像块降采样至20×或10×，再通过插值或深度学习SR方法重建至原始尺寸，然后使用固定的WSS管道进行分割评估。对比不同退化水平、不同重建方法对分割性能的影响，并分析重建质量与分割性能的关系。
- **关键技术细节**：
  1. **降采样与重建流程**：
     - 原始图像块：224×224像素（40×）。
     - 模拟低倍镜：20×（112×112）和10×（56×56），像素数分别降为1/4和1/16。
     - 重建方法：
       - **双线性插值**：无学习，直接上采样回224×224。
       - **STAR-RL**：病理导向的深度SR模型，基于时空分层强化学习，动作空间包括边缘增强、对比增强、平滑、锐化等操作。因目标图像为224×224而模型输入为192×192，需经过两次双三次插值调整大小。
  2. **弱监督分割管道**：采用WSSS-Tissue框架，该框架利用图像级多标签训练分类网络生成CAM伪标签，再训练分割网络。使用渐进式Dropout注意力（PDA）、多层伪监督（MLPS）和分类门控（Classification Gate）改进定位。本研究中保持所有超参数和训练设置不变，仅改变输入图像来源。
- **公式/算法流程（文字描述）**：
  - 低倍镜图像 → [插值或STAR-RL] → 重建40×图像 → WSSS-Tissue训练（分类→分割） → 评估IoU等指标。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **LUAD-HistoSeg**：肺腺癌弱监督组织分割数据集。训练集含补丁级多标签，测试集有像素级掩码。类别：肿瘤上皮(TE)、肿瘤相关间质(TAS)、坏死(NEC)、淋巴细胞(LYM)。原始尺寸224×224（40×）。
  - **BCSS-WSSS**：源自乳腺癌症分割数据集，转为弱监督格式。类别：肿瘤(TUM)、间质(STR)、淋巴细胞浸润(LYM)、坏死(NEC)。原始尺寸224×224。
- **基准**：原始40×图像作为参考基准（上界），对比四种重建设置：20×→40×双线性插值、20×→40× STAR-RL、10×→40×双线性插值、10×→40× STAR-RL。
- **对比方法**：双线性插值（传统方法）和STAR-RL（病理导向深度SR方法）。无其他WSS方法对比（因为固定使用WSSS-Tissue），重点在于不同输入设置下的差异。

## 4. 资源与算力

- **论文未明确说明具体GPU型号、数量、训练时长**。仅提到STAR-RL在HistoSR数据集上训练（192×192图像），然后应用于目标数据集。WSSS-Tissue训练和评估均在固定配置下进行。未提供详细算力开销分析。

## 5. 实验数量与充分性

- **实验数量**：两个数据集（LUAD-HistoSeg和BCSS-WSSS），每个数据集下有5种输入设置（原始40× + 4种重建），共10组实验。每组实验采用相同WSSS-Tissue配置，独立训练和评估。提供了定量结果（表I）和定性可视化（图2）。同时给出了重建质量指标对比（图3）。
- **充分性评估**：
  - **优点**：控制变量严格（仅改变输入来源），对比了两种退化水平和两种重建方法，覆盖了轻度和重度退化。在两个不同组织（肺腺癌和乳腺癌）的数据集上验证，增强了结论的泛化性。
  - **不足**：
    - 仅使用一个WSS管道（WSSS-Tissue），未测试其他弱监督分割方法（如SEAM、SC-CAM、HisynSeg等），可能无法完全推广到不同WSS方法。
    - 仅使用一种深度SR方法（STAR-RL），未与更多通用SR方法（如ESPCN、SRGAN等）对比。
    - 未对重建方法进行消融研究（如只使用双三次插值作为基准，而STAR-RL需要两次双三次插值调整大小，可能引入额外影响）。
    - 未分析不同类别在不同退化下的边界变化细节（仅有IoU数值，缺乏误差分析如F1、精确率、召回率等）。

## 6. 论文的主要结论与发现

1. **适度退化（20×→40×）可行**：在BCSS-WSSS上，双线性插值甚至略超原始40×性能（mIoU 69.04% vs 68.87%），表明对于大而连续的组织区域，两倍分辨率降低可被容忍且几乎无损失。在LUAD-HistoSeg上则下降较多（mIoU降5.48个百分点），但STAR-RL可部分恢复（降3.19个百分点）。
2. **严重退化（10×→40×）导致性能崩溃**：尤其在LUAD-HistoSeg上，淋巴细胞（LYM）类别IoU归零，说明小尺度细胞结构的判别信息被完全消除，后处理重建无法恢复。
3. **STAR-RL优势依赖上下文**：在更困难的设置（LUAD-HistoSeg、10×退化）中STAR-RL优于插值；但在轻度退化（BCSS-WSSS 20×）中插值反而轻微更好。因此，深度SR的优势并非普遍适用。
4. **重建质量指标不能预测分割性能**：尽管STAR-RL获得更高PSNR、SSIM等，但分割mIoU不一定更高，甚至出现下降。需要任务驱动而非仅凭图像质量评估。
5. **存在关键退化点**：即小尺度结构（如淋巴细胞）在四倍降采样后完全失效，提示存储系统需根据目标组织类型选择合适倍率。

## 7. 优点：方法或实验设计上的亮点

- **问题新颖**：首次系统研究低倍镜存储与弱监督分割的结合，同时考虑存储效率和标注成本。
- **任务驱动评估**：不局限于重建图像质量，而是直接测量下游分割性能，更贴近实际应用需求。
- **控制变量严格**：WSS管道完全固定，仅改变输入来源，确保比较公平。
- **双数据集验证**：涵盖肺腺癌和乳腺癌两种组织，增加了结论的稳健性。
- **定量+定性结合**：提供IoU表和可视化分割图，直观展示退化对不同类别的影响。
- **实用指导**：明确指出不同退化水平对不同组织类型的影响差异，为实际存储系统设计提供依据。

## 8. 不足与局限

- **仅一种WSS方法**：WSSS-Tissue是特定方法，其CAM定位机制可能对低分辨率敏感，其他方法（如基于视觉Transformer、自监督一致性等）可能表现不同。结论的泛化性有限。
- **仅一种深度SR方法**：未比较通用SR（如EDSR、RCAN）或其他病理SR（如PathSRGAN），无法全面评估SR的收益。
- **STAR-RL的预处理偏差**：调整大小操作（bicubic → 192 → bicubic → 224）引入了额外插值，可能掩盖部分效果。建议使用输入尺寸与目标匹配的SR模型。
- **缺少因果分析**：对于淋巴细胞类别失效，仅观察结果，未深入分析CAM激活图的退化机制（如激活面积、置信度变化）。
- **未考虑噪声/压缩等实际存储因素**：仅模拟分辨率降低，实际存储还涉及压缩、格式转换等，可能引入不同退化。
- **未讨论计算开销**：STAR-RL作为强化学习方法，推理时间可能远高于插值，论文未比较效率。对于大规模WSI，实时性可能需要权衡。
- **两个数据集规模较小**：均为公开补丁级数据集，可能无法完全代表临床全切片中的复杂异质性。

（完）
