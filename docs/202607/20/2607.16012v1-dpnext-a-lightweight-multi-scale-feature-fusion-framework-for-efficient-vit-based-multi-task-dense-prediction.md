---
title: "DPNeXt: A Lightweight Multi-Scale Feature Fusion Framework for Efficient ViT-Based Multi-Task Dense Prediction"
title_zh: DPNeXt：一种轻量级多尺度特征融合框架，用于基于ViT的高效多任务密集预测
authors: "Jehun Kang, Jungha Wang, Youngjun Hwang, David Hyunchul Shim"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16012v1"
tags: ["query:seg-llm"]
score: 9.0
evidence: 包含语义分割的多任务密集预测
tldr: "针对多任务密集预测中ViT解码效率低的瓶颈，提出DPNeXt轻量框架。其核心是双深度可分离倒置瓶颈解码器和多任务边界引导策略，在减少参数与计算的同时提升特征融合与任务解耦。在Cityscapes和NYUv2上实现语义分割与深度估计最优，参数较DPT减少78.6%，推理速度最快，适用于资源受限场景。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16012v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 972, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1832, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 897, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1751, \"height\": 1917, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16012v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 904, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 478, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 898, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 463, \"label\": \"Table\"}]"
motivation: 现有ViT解码策略计算量大且任务间负迁移严重，亟需高效轻量的多尺度特征融合方法。
method: 设计双深度可分离倒置瓶颈结构实现融合解码，并引入边界监督信号约束任务特异性，提升几何一致性。
result: "Cityscapes上达SOTA，NYUv2上分割与深度最优，参数减少78.6%，推理速度领先。"
conclusion: DPNeXt以极低开销达成高效多任务密集预测，适合实时与边缘部署。
---

## 摘要
机器人感知系统中的多任务学习（MTL）通过整合语义分割和深度估计来支持全面的3D空间场景理解。尽管视觉基础模型（VFM）越来越多地被用作稳健的特征编码器，但现有的解码策略存在关键瓶颈。为此，我们提出了DPNeXt，一种流线型的多尺度特征融合解码器，是标准密集预测变换器（DPT）的高效替代方案。DPNeXt使用双深度可分离倒置瓶颈，通过以融合为中心的解码和独立任务模块化来改进冻结VFM的利用。为了进一步缓解任务间的负归纳迁移，我们引入了多任务边界引导（MTBG）策略。与以往添加融合模块或门控的边界感知方法不同，MTBG应用对称的边界聚焦监督来鼓励几何一致性，无需额外的标注或推理成本。在Cityscapes上的实验表明，DPNeXt-S优于先前最先进的MTL模型，而DPNeXt-B进一步提升了整体性能，并在所比较的方法中取得了最佳结果。在NYUv2上，DPNeXt-B在所比较的方法中也取得了最佳的语义分割和深度估计结果，同时所需的可训练参数远少于先前的大规模MTL模型。与标准DPT相比，DPNeXt-S减少了78.6%的可训练参数，并在资源受限的笔记本电脑硬件上实现了所比较模型中最快的推理速度。源代码、模型检查点和演示视频将在https://github.com/kangjehun/DPNeXt上提供。

## Abstract
Multi-Task Learning (MTL) in robotics perception systems supports comprehensive 3D spatial scene understanding by integrating semantic segmentation and depth estimation. While Vision Foundation Models (VFMs) are increasingly adopted as robust feature encoders, existing decoding strategies present a critical bottleneck. To address this, we propose DPNeXt, a streamlined multi-scale feature fusion decoder and efficient alternative to the standard Dense Prediction Transformer (DPT). DPNeXt uses dual depthwise separable inverted bottlenecks to improve frozen VFM utilization through fusion-centric decoding and independent task modularization. To further mitigate negative inductive transfer between tasks, we introduce the Multi-Task Boundary Guidance (MTBG) strategy. Unlike prior boundary-aware methods that add fusion modules or gating, MTBG applies symmetric boundary-focused supervision to encourage geometric consistency without extra annotation or inference cost. Experiments on Cityscapes show that DPNeXt-S outperforms prior state-of-the-art (SOTA) MTL models, while DPNeXt-B further improves the overall performance and achieves the best results among the compared methods. On NYUv2, DPNeXt-B also achieves the best semantic segmentation and depth estimation results among the compared methods while requiring substantially fewer trainable parameters than prior large-scale MTL models. Compared with the standard DPT, DPNeXt-S reduces trainable parameters by 78.6% and achieves the fastest inference speed among the compared models on resource-constrained laptop hardware. The source code, model checkpoints, and a demo video will be made available at https://github.com/kangjehun/DPNeXt.

---

## 论文详细总结（自动生成）

### 论文结构化中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：机器人感知系统需要多任务密集预测（如语义分割+深度估计），视觉基础模型（VFM，如DINOv2）可作为强大特征编码器，但现有解码策略（尤其是Dense Prediction Transformer, DPT）计算量大、参数多，且任务间存在负归纳迁移，成为性能瓶颈。
- **核心问题**：如何设计轻量、高效的解码器，同时缓解多任务学习中的任务冲突，使其在资源受限设备（如边缘机器人）上实现实时密集预测。
- **整体含义**：本文提出DPNeXt框架，通过高效的纯卷积多尺度融合解码器（DPNeXt）和零推理开销的任务平衡策略（MTBG），在不牺牲精度的前提下大幅降低参数量和计算延迟，为VFM在机器人领域的实际部署提供了可行方案。

#### 2. 方法论：核心思想、关键技术细节、公式/算法流程
- **核心思想**：
  - 使用冻结的DINOv2-Reg backbone提取多尺度特征。
  - 设计DPNeXt解码器：采用**双深度可分离倒置瓶颈（DDSIF）** 替代DPT中的密集卷积残差融合单元，实现轻量化多尺度特征融合。
  - 引入**多任务边界引导（MTBG）**：通过对称边界监督（语义边界&深度边界）鼓励几何一致性，缓解任务负迁移，训练时使用辅助边界头，推理时丢弃，零额外成本。
- **关键技术细节**：
  - **IPA模块**：避免人工通道扩展（原DPT常见做法），直接将各层特征投影到统一融合维度（256），保持原有分辨率层次。
  - **DDSIF块**：两个阶段，每阶段由深度卷积+逐点卷积组成，第二阶段使用倒置瓶颈（先通道扩展再压缩），扩展比r=2。
  - **MTBG策略**：
    - 边界标签可从语义分割标签通过Canny边缘检测+膨胀获得。
    - 辅助边界损失：加权二值交叉熵（针对空间不平衡动态计算权重）。
    - 边界感知损失：仅在边界区域施加分割损失和深度损失。
- **公式/算法流程（文字说明）**：
  1. 输入图像经冻结DINOv2-Reg，取第3、6、9、12层特征，维度均为d_backbone×Hp×Wp（Hp=H/14）。
  2. 通过IPA将各层投影到dfusion=256，并调整空间分辨率（sk=4,2,1,1/2）。
  3. 通过DDSIF块从深层到浅层递归融合：先上采样2倍，再与当前层特征融合。
  4. 最终共享特征P1被送入独立任务头（分割、深度、边界辅助头）。
  5. 总损失 = λsegLseg + λdepthLdepth + λboundLbound + λbasLbas + λbadLbad（其中Lseg用OHEM交叉熵，Ldepth用SiLog损失+归一化深度，Lbas/Lbad为边界感知损失）。
  6. 训练时包含边界辅助头，推理时只保留分割和深度头。

#### 3. 实验设计
- **数据集与场景**：
  - **Cityscapes**：城市驾驶场景，2975训练/500验证，19类语义+深度（0.001-80m，使用CREStereo深度图）。
  - **NYUv2**：室内RGB-D，795训练/654测试，40类语义+深度（0.001-10m）+表面法线辅助任务（仅训练用）。
- **基准（Benchmark）**：
  - 主要指标：语义分割mIoU（%），深度估计RMSE（m），综合指标JPS（Joint Performance Score）。
  - 对比方法：PAD-Net、MTL、DenseMTL、3-ways、SwinMTL、DPT-DINOv2-S、M2H、InvPT、TaskPrompter、MTMamba++、MLoRE等。
  - 额外对比计算效率：GFLOPs、FPS（在RTX 2080 Laptop GPU上实测）。
- **对比方式**：
  - Cityscapes：全分辨率1024×2048，滑动窗口（518×518，重叠0.5，高斯融合），单尺度，无TTA。
  - NYUv2：448×576单次前向，无滑动窗口/翻转/多尺度。
  - 尽量采用公开可复现结果，部分基线使用官方代码或第三方复现。

#### 4. 资源与算力
- **训练硬件**：
  - 主流实验：单张NVIDIA RTX 3090 GPU（训练主benchmark 400 epoch，batch size=8）。
  - 消融实验：四张NVIDIA Titan RTX GPU（加速实验迭代，160 epoch，batch size=32）。
- **推理测速**：NVIDIA RTX 2080 Laptop GPU（模拟资源受限场景）。
- **训练时长**：未明确给出具体小时数，但可根据epoch和batch估算（400 epoch on Cityscapes，512×512 crop，8 batch → 约1-2天）。
- **注意事项**：论文未详细列出每次实验的精确运行时间，但提供了FPS数据。

#### 5. 实验数量与充分性
- **实验组数**：
  - 主结果：两个数据集（Cityscapes、NYUv2）共4组（DPNeXt-S/B分别对比）。
  - 计算效率对比：7种方法（参数、GFLOPs、FPS）。
  - 消融实验：一个表（表IV）包含两类：
    - 从标准DPT→IPA→DDSIF(双深度可分离)→DDSIF(倒置瓶颈)共4行。
    - MTBG策略：OHEM、BAS、BAD不同组合共4行。
  - 定性对比：图5展示Cityscapes场景。
- **充分性与公平性**：
  - 消融逐步拆解了DPNeXt各组件贡献（IPA、DDSIF、倒置瓶颈），清晰。
  - MTBG消融分别验证了不对称边界引导的效果，发现BAS单独会伤害深度，BAD单独提升深度，全结合最优。
  - 对比方法较为全面，涵盖近期SOTA（2023-2025），包括基于Transformer、Mamba、专家混合等方法。
  - 计算效率测试在相同硬件下进行，公平。
  - 但仅使用了单一backbone（DINOv2-S/B），未验证在其他VFM（如SAM、DINOv1）上的泛化性；且只做了两个主要任务（+NYUv2的表面法线辅助），未涉及更多密集任务（如表面法线作为主任务）。

#### 6. 主要结论与发现
- **性能领先**：DPNeXt-B在Cityscapes和NYUv2上均取得最佳JPS（0.867和0.789），分割mIoU和深度RMSE均为最优或次优。
- **轻量高效**：DPNeXt-S在Cityscapes上相比标准DPT减少78.6%可训练参数，且推理速度最快（51.02 FPS）。
- **MTBG有效**：对称边界监督提升了整体性能（JPS从0.810→0.814），且零推理开销。
- **实践意义**：纯CNN操作天然适应硬件优化，避免了自定义操作（如扫描、动态路由）带来的实际延迟，适合资源受限设备。

#### 7. 优点
- **方法创新**：
  - 将现代CNN骨干的高效设计（深度可分离、倒置瓶颈）引入ViT解码器，实现轻量融合。
  - MTBG以极低成本（仅增加边界训练头，推理丢弃）缓解负迁移，且无需额外标注（可从标签派生）。
  - 解耦任务头，保持模块化，便于扩展新任务。
- **实验充分**：涵盖两个经典数据集、多种SOTA基线、计算效率实测、详细消融。
- **实用导向**：强调在真实硬件（笔记本GPU）上的推理速度，而非仅理论FLOPs。
- **代码开源**：承诺提供代码、检查点和演示视频，可复现。

#### 8. 不足与局限
- **实验覆盖局限**：
  - 仅使用DINOv2-Reg一种backbone，未验证在其他VFM（如DINOv1、CLIP、SAM）上的表现。
  - 只评估了语义分割+深度两个主任务，未扩展到其他密集预测（如边界检测、表面法线作为主任务、光流等）。
  - 未在更极端资源受限平台（如Jetson、手机）上测试。
- **潜在偏差风险**：
  - Cityscapes的深度标签来自CREStereo伪标签，可能存在域差异。
  - 消融实验使用更短的训练轮次（160 vs 400）和多GPU，超参数可能未充分针对消融调优。
  - 部分基线（如M2H）由于代码不可用，无法完全公平复现，使用了作者报告值，可能优于实际结果。
- **应用限制**：
  - 方法依赖冻结的VFM（DINOv2），虽然降低了训练成本，但限制了在特定小数据集上的微调灵活性。
  - 当前只处理单帧图像，未考虑时序信息（未来工作提到流式感知，但未在当前论文实现）。
  - 边界监督依赖于语义标签的准确提取，若分割粗糙则可能引入噪声。

（完）
